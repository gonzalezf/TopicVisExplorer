# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.env
.venv
.venv-legacy/
.venv-next/
env/
venv/
ENV/

# Caches and tooling
.cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover
.hypothesis/
.tox/
.nox/

# Editor / OS
.vscode/
.idea/
.DS_Store
desktop.ini

# Notebook
.ipynb_checkpoints/

# Build outputs
# Vite output (regenerated by `cd frontend && npm run build`). The legacy
# subfolder under web/ IS committed because it's vendored paper-version
# assets; only web/dist/ is generated.
src/topicvisexplorer/web/dist/
frontend/dist/
frontend/node_modules/
frontend/playwright-report/
frontend/test-results/
frontend/.vite/
frontend/npm-install.log
node_modules/

# Stray top-level npm artifacts (only the per-project frontend/ should
# contain a package.json; if these appear at the repo root it's npm
# misresolution and should be removed).
/package.json
/package-lock.json

# Local data
*.pkl
*.kv
*.npy
!tests/data/*.pkl
!tests/data/*.kv
!tests/data/*.npy
!tests/golden/**/*.json

# Docs site build
site/

# Paper review materials kept locally only
paper_in_review/
